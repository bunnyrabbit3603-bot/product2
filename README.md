# Lotto Number Generator

간단한 정적 웹앱입니다.

## 기능
- 1~45 사이 숫자 6개를 중복 없이 생성
- 오름차순 정렬 후 번호 볼 UI로 표시
- 생성 횟수와 마지막 생성 번호 표시

## 실행
`index.html`을 브라우저에서 열면 바로 동작합니다.

## Cloudflare 배포
- 배포 설정: `wrangler.jsonc`
- 배포 명령: `npx wrangler deploy`
